general:
  branches:
    only:
      - master

machine:
  timezone:
    America/Los_Angeles

test:
  override:
    - echo "No tests are needed"

deployment:
  production:
    branch: master
    commands:
      - ssh $SSHU "cd $DIR && git pull -u origin master && bundle exec gulp && cd build && forever restartall"
